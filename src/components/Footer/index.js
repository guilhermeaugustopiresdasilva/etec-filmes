import styles from "./Footer.module.css";

function Footer() {
    return (
        <footer className={styles.footer}>
            <h2>GuigaFlix &copy; Desenvolvido por Guilherme Augusto</h2>
        </footer>
    );
}

export default Footer;
